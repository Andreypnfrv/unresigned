{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "yarn install && yarn build"
  },
  "deploy": {
    "startCommand": "yarn next start -H 0.0.0.0 -p ${PORT}"
  }
}
